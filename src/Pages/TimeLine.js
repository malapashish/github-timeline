import React from 'react';

const TimeLine = ({props}) => {
    console.log(props);
    return(
        <div>
            Hello from timeline component
        </div>
    )
}

export default TimeLine;